import fetchGamesForQuickStats from "./fetchGamesForQuickStats";

export default async function compareFetchedGames(leagueId, lastGame, nextGameId, today, sortedByDate) {
    try {
        let nextGame = fetchGamesForQuickStats(leagueId, nextGameId)
        while (condition) {
            // ... missing code:
        }
    } catch (error) {
        
    }
}